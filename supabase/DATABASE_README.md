# Database README Notice

This file is kept only as a pointer.

Use the new canonical DB workflow documentation:

- `supabase/db/README.md`

The new workflow is based on local Docker Supabase backup/restore scripts and a strict data exclusion policy.
