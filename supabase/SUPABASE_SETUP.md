# Supabase Setup Notice

This file is kept only as a pointer.

Use the new canonical DB workflow documentation:

- `supabase/db/README.md`

That document contains the exact backup/restore flow for local Docker Supabase, including inclusion/exclusion policy for data.
